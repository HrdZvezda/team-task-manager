# Core Flask
Flask==3.0.0
flask-cors==4.0.0

# Database
Flask-SQLAlchemy==3.1.1
psycopg2-binary==2.9.9  # PostgreSQL adapter (如果用 PostgreSQL)
# 或者用 SQLite 就不需要額外的 adapter

# Authentication & Security
Flask-JWT-Extended==4.6.0
Flask-Bcrypt==1.0.1

# Input Validation
marshmallow==3.20.1

# Rate Limiting
Flask-Limiter==3.5.0
redis==5.0.1  # Rate limiting 的後端 (production 環境)

# Utilities
python-dotenv==1.0.0  # 管理環境變數

# Development
pytest==7.4.3  # 測試框架
pytest-cov==4.1.0  # 測試覆蓋率
black==23.12.1  # 程式碼格式化
flake8==7.0.0  # Linting
mypy==1.8.0  # Type checking

# Production Server
gunicorn==21.2.0  # WSGI server (production)